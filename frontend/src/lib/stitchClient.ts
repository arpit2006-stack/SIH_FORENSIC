import * as Realm from "realm-web";

// Initialize the Realm App with the placeholder App ID.
// Replace 'sih-forensic-app-xyz' with your actual App ID from MongoDB App Services.
export const app = new Realm.App({ id: "sih-forensic-app-xyz" });

export default app;
